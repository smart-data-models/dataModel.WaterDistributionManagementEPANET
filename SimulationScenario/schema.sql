/* (Beta) Export of data model SimulationScenario of the subject dataModel.WaterDistributionManagementEPANET for a PostgreSQL database. Pending translation of enumerations and multityped attributes */
CREATE TYPE demandModel_type AS ENUM ('DDA','PDA');CREATE TYPE flowUnits_type AS ENUM ('AFD','CFS','CMD','CMH','GPM','IMGD','LPS','LPM','MLD','MGD');CREATE TYPE headlossFormula_type AS ENUM ('H-W','D-W','C-M');CREATE TYPE initialStatus_type AS ENUM ('OPEN','CLOSED','CV');CREATE TYPE qualityType_type AS ENUM ('age','chem','none','trace');CREATE TYPE statistic_type AS ENUM ('averaged','maximum','minimum','none','range');CREATE TYPE status_type AS ENUM ('OPEN','CLOSED','CV');CREATE TYPE SimulationScenario_type AS ENUM ('SimulationScenario');CREATE TYPE unbalanced_type AS ENUM ('stop','continue','continue_N');CREATE TYPE valveType_type AS ENUM ('FCV','GPV','PBV','PRV','PSV','TCV');
CREATE TABLE SimulationScenario (accuracy NUMERIC, address JSON, alternateName TEXT, areaServed TEXT, bulkOrder NUMERIC, checkFrequency NUMERIC, chemicalName TEXT, chemicalUnits TEXT, concentrationLimit NUMERIC, dampLimit NUMERIC, dataProvider TEXT, dateCreated TIMESTAMP, dateModified TIMESTAMP, demandCategory JSON, demandCharge NUMERIC, demandModel demandModel_type, description TEXT, diffusivity NUMERIC, duration NUMERIC, emitterExponent NUMERIC, energyUse JSON, flow JSON, flowChange NUMERIC, flowUnits flowUnits_type, head JSON, headError NUMERIC, headlossFormula headlossFormula_type, hydraulicTimeStep NUMERIC, initialQuality JSON, initialStatus initialStatus_type, inputParameter JSON, level JSON, maxCheck NUMERIC, minimumPressure NUMERIC, name TEXT, operationalControl JSON, owner JSON, patternStart TIMESTAMP, patternStep NUMERIC, pressure JSON, pressureExponent NUMERIC, quality JSON, qualityTimeStep NUMERIC, qualityType qualityType_type, reportStart NUMERIC, reportStep NUMERIC, requiredPressure NUMERIC, ruleTimeStep NUMERIC, source TEXT, sourceCategory JSON, sourceMassInflow JSON, specificGravity NUMERIC, startClockTime NUMERIC, statistic statistic_type, status status_type, supply JSON, tag TEXT, tankOrder NUMERIC, tolerance NUMERIC, trials NUMERIC, type SimulationScenario_type, unbalanced unbalanced_type, unbalancedN NUMERIC, valveType valveType_type, velocity JSON, viscosity NUMERIC, wallOrder NUMERIC);